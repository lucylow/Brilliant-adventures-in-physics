import { useEffect, useState } from "react";
import { router } from "expo-router";
import { ScrollView, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { Card, EmptyState, MasteryRing, Pill, PrimaryButton, ProgressBar, SectionHeader } from "@/components/physica-ui";
import { loadLearningState, type LearningState } from "@/lib/progress-store";
import { useColors } from "@/hooks/use-colors";

export default function HomeScreen() {
  const colors = useColors();
  const [learning, setLearning] = useState<LearningState>({ attempts: 0, correct: 0, savedQuestions: [], topics: {}, streak: 0 });
  useEffect(() => { void loadLearningState().then(setLearning); }, []);
  const accuracy = learning.attempts ? learning.correct / learning.attempts : 0;
  return <ScreenContainer className="p-5"><ScrollView contentContainerStyle={{ paddingBottom: 32 }} showsVerticalScrollIndicator={false}><View style={{ gap: 22 }}><View><Text style={{ fontSize: 30, fontWeight: "800", color: colors.foreground }}>Hi, physicist</Text><Text style={{ marginTop: 5, color: colors.muted, fontSize: 16 }}>Let’s understand some physics today.</Text></View><Card><Pill label="TODAY’S PLAN" active /><Text style={{ marginTop: 12, fontSize: 22, fontWeight: "800", color: colors.foreground }}>Master projectile motion</Text><Text style={{ marginTop: 7, color: colors.muted, lineHeight: 21 }}>One concept, one simulation, one practice set.</Text><View style={{ marginTop: 16 }}><PrimaryButton label="Start today’s plan" onPress={() => router.push("/lab" as never)} /></View></Card><View><SectionHeader title="Quick actions" subtitle="Choose a focused way to learn." /><View style={{ flexDirection: "row", flexWrap: "wrap", gap: 10 }}><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/tutor" as never)}><Text style={{ fontSize: 24 }}>✦</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Ask Tutor</Text></Card><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/practice" as never)}><Text style={{ fontSize: 24 }}>✓</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Practice</Text></Card><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/lab" as never)}><Text style={{ fontSize: 24 }}>◌</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Physics Lab</Text></Card><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/scan" as never)}><Text style={{ fontSize: 24 }}>⌁</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Scan</Text></Card><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/lens" as never)}><Text style={{ fontSize: 24 }}>◉</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Lens</Text></Card><Card style={{ flex: 1, minWidth: "30%" }} onPress={() => router.push("/lesson" as never)}><Text style={{ fontSize: 24 }}>▤</Text><Text style={{ marginTop: 8, fontWeight: "800", color: colors.foreground }}>Lesson</Text></Card></View></View><Card><View style={{ flexDirection: "row", alignItems: "center", gap: 16 }}>{learning.attempts ? <MasteryRing value={accuracy} /> : <View style={{ width: 76, height: 76, borderRadius: 38, backgroundColor: colors.primary + "14", alignItems: "center", justifyContent: "center" }}><Text style={{ color: colors.primary, fontWeight: "800" }}>NEW</Text></View>}<View style={{ flex: 1 }}><Text style={{ fontSize: 18, fontWeight: "800", color: colors.foreground }}>Your physics map</Text><Text style={{ marginTop: 4, color: colors.muted }}>{learning.attempts ? `${learning.correct} of ${learning.attempts} practice attempts correct.` : "Complete your first practice attempt to start your progress map."}</Text><View style={{ marginTop: 12 }}><ProgressBar value={accuracy} /></View></View></View></Card><View><SectionHeader title="Recent activity" action="View all" onAction={() => router.push("/progress" as never)} />{learning.attempts ? <Card onPress={() => router.push("/practice" as never)}><Text style={{ fontWeight: "800", color: colors.foreground }}>Continue {learning.lastTopic ?? "practice"}</Text><Text style={{ marginTop: 5, color: colors.muted }}>Your latest attempt is ready for a focused follow-up.</Text></Card> : <Card><EmptyState title="No activity yet" body="Ask your first question or start a practice set to see your learning history here." action={<PrimaryButton label="Start practice" onPress={() => router.push("/practice" as never)} />} /></Card>}</View></View></ScrollView></ScreenContainer>;
}
